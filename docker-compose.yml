version: "3.9"

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: rag_backend
    ports:
      - "8000:8000"
    volumes:
      # Hot-reload: mount source so --reload picks up changes
      - ./backend:/app
      # Persist Chroma vector data between restarts
      - rag_chroma:/app/chroma_data
    env_file:
      - ./backend/.env.local
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: rag_frontend
    ports:
      - "3001:3000"
    volumes:
      # Hot-reload: mount source so next dev picks up changes
      - ./frontend:/app
      # Keep node_modules inside the container, not overwritten by the host mount
      - /app/node_modules
      # Keep .next inside the container — host bind mount would replace it with a
      # Windows/macOS .next (or none), breaking webpack chunks (e.g. ./403.js 404).
      - /app/.next
    env_file:
      - ./frontend/.env.local
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000
      # Windows + bind mounts: more reliable file watching (reduces odd HMR 404s)
      - WATCHPACK_POLLING=true
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  rag_chroma:
